#include <iostream>

int main() {
  // Create a variable x
  // Assign it value of 10
  int x = 10;

  // if x > 5, then print "x is greater than "
  if (x > 5) {
    std::cout << "x is greater than 5\n";
  }

  return 0;
}
